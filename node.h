#ifndef node_h
#define node_h

#include "cs.h"

struct node{
node(){yield = 0; profit = 0; profit =0; Tcost =0; balance = 900000; }
std::vector<stockInfo> stockVector; 
double yield ; 
double profit; 
double Tcost ; 
double balance; 
};
 #endif 